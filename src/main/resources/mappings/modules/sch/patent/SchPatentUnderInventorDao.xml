<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.krazy.kcfw.modules.sch.dao.patent.SchPatentUnderInventorDao">
    
	<sql id="schPatentUnderInventorColumns">
		a.id AS "id",
		a.spi_patent_id AS "spiPatent.id",
		a.spi_type_code AS "spiTypeCode",
		c.label AS "spiTypeName",
		a.spi_user_id AS "spiUserId",
		u5.name AS "spiUserName",
		a.spi_teacher_id AS "spiTeacherId",
		u6.name AS "spiTeacherName",
		a.SPI_USER_NAME_EX AS "spiUserNameEX",
		a.spi_user_office_id AS "spiUserOfficeId",
		o7.name AS "spiUserOfficeName",
		a.spi_teacher_office_id AS "spiTeacherOfficeId",
		o8.name AS "spiTeacherOfficeName",
		a.spi_office_name_ex AS "spaOfficeNameEx",
		a.spi_contribution_per AS "spiContributionPer",
		a.spi_remark AS "spiRemark",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="schPatentUnderInventorJoins">
		LEFT JOIN sch_patent_under b ON b.id = a.spi_patent_id
		LEFT JOIN sys_user u5 ON u5.id = a.spi_user_id
		LEFT JOIN sys_user u6 ON u6.id = a.spi_teacher_id
		LEFT JOIN sys_office o7 ON o7.id = a.spi_user_office_id
		LEFT JOIN sys_office o8 ON o8.id = a.spi_teacher_office_id
		left join sys_dict c on c.type='INVENTOR_TYPE' and c.value=a.spi_type_code
	</sql>
    
	<select id="get" resultType="SchPatentUnderInventor">
		SELECT 
			<include refid="schPatentUnderInventorColumns"/>
		FROM sch_patent_under_inventor a
		<include refid="schPatentUnderInventorJoins"/>
		WHERE a.spi_id = #{spiId}
	</select>
	
	<select id="findList" resultType="SchPatentUnderInventor">
		SELECT 
			<include refid="schPatentUnderInventorColumns"/>
		FROM sch_patent_under_inventor a
		<include refid="schPatentUnderInventorJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="spiPatent.id != null and spiPatent.id != ''">
				AND a.spi_patent_id = #{spiPatent.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date ASC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SchPatentUnderInventor">
		SELECT 
			<include refid="schPatentUnderInventorColumns"/>
		FROM sch_patent_under_inventor a
		<include refid="schPatentUnderInventorJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date ASC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO sch_patent_under_inventor(
			id,
			spi_patent_id,
			spi_type_code,
			spi_user_id,
			spi_teacher_id,
			spi_user_name_ex,
			spi_user_office_id,
			spi_teacher_office_id,
			spi_office_name_ex,
			spi_contribution_per,
			spi_remark,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag
		) VALUES (
			#{id},
			#{spiPatent.id},
			#{spiTypeCode},
			#{spiUserId},
			#{spiTeacherId},
			#{spiUserNameEX},
			#{spiUserOfficeId},
			#{spiTeacherOfficeId},
			#{spiOfficeNameEx},
			#{spiContributionPer},
			#{spiRemark},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE sch_patent_under_inventor SET 	
			spi_patent_id = #{spiPatent.id},
			spi_type_code = #{spiTypeCode},
			spi_user_id = #{spiUserId},
			spi_teacher_id  = #{spiTeacherId},
			spi_user_name_ex = #{spiUserNameEX},
			spi_user_office_id = #{spiUserOfficeId},
			spi_teacher_office_id = #{spiTeacherOfficeId},
			spi_office_name_ex = #{spiOfficeNameEx},
			spi_contribution_per = #{spiContributionPer},
			spi_remark = #{spiRemark},
			update_by = #{updateBy.id},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>
<!-- 	
	<update id="delete">
		UPDATE sch_patent_under_inventor SET 
			del_flag = #{DEL_FLAG_DELETE}
		<choose>
			<when test="id !=null and id != ''">
				WHERE id = #{id}
			</when>
			<otherwise>
				WHERE spi_patent_id = #{spiPatentId}
			</otherwise>
		</choose>
	</update>
 -->	
	<update id="delete">
		DELETE FROM sch_patent_under_inventor
		WHERE spi_patent_id =  #{spiPatent.id}
	</update>
	
</mapper>